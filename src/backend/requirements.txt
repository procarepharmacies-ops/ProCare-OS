# ProCare OS backend runtime.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
SQLAlchemy>=2.0,<3.0      # ProCare's own DB (SQLite in dev, SQL Server in prod)
httpx>=0.27              # Claude API call (assistant) + WhatsApp Cloud API client
python-dateutil>=2.9

# --- Test / dev ---
pytest>=8.0

# --- Production SQL Server (uncomment when pointing at the real ProCare DB) ---
# pyodbc>=5.1            # needs "ODBC Driver 18 for SQL Server"; auto-used when
#                         config/connections.json:procare_database has real creds

# --- Optional, documented upgrades (not required to run) ---------------------
# APScheduler>=3.10      # in-process scheduler for expiry_alerts / auto_purchase_order
# prophet>=1.1           # replaces the moving-average forecast in services/alerts.py
# anthropic>=0.40        # alternative to the raw httpx Claude call in services/ai.py
